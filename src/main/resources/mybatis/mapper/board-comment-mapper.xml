<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.mapper.BoardCommentMapper">
	<select id="boardCommentListData" resultType="com.sist.web.vo.BootCommentVO" parameterType="hashmap">
		SELECT no, board_no, userid, name, msg, to_char(regdate, 'yyyy-mm-dd hh24:mi:ss') as dbday, group_tab
		FROM bootcomment
		WHERE board_no = #{board_no}
		ORDER BY group_id DESC, group_step ASC
		OFFSET #{start} ROWS FETCH NEXT 10 ROWS ONLY
	</select>
	<select id="boardCommentCount" resultType="int" parameterType="int">
		SELECT count(*)
		FROM bootcomment
		WHERE board_no = #{board_no}
	</select>
	<insert id="boardCommentInsert" parameterType="com.sist.web.vo.BootCommentVO">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			SELECT NVL(MAX(no)+1,1) as no FROM bootcomment
		</selectKey>
		INSERT INTO bootcomment(no, board_no, userid, name, msg, group_id)
		VALUES(#{no}, #{board_no}, #{userid}, #{name}, #{msg},(select nvl(max(group_id)+1,1) from bootcomment))
	</insert>
	
</mapper>