version: "3.8"
services:
  app:
    build: . # . DockerFile을 사용해서 이미지를 새로 빌드
    image: jeunju528/pinia-app:latest # 빌드한 이미지에 이름(태그)을 새로 붙이는 것, build가 없으면 이 이름의 이미지를 가져와 실행
    container_name: pinia-app
    ports:
      - "9090:9090"
    env_file:
      - .env
    restart: always